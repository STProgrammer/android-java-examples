<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Dialog-stiler -->
    <style name="AlertDialogCustom" parent="Theme.AppCompat.Light.Dialog">

    </style>

    <style name="MyDialogTitleStyle" parent="android:Widget.TextView">
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:padding">15dp</item>
        <item name="android:textSize">22sp</item>
        <item name="android:textColor">#FFFFFFFF</item>
    </style>

    <style name="MyActivityDialogStyle" parent="Theme.AppCompat.Light.Dialog">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>
        <item name="android:textColor">#ff00ed00</item>
        <item name="android:textSize">18sp</item>
        <item name="android:titleTextStyle">@style/Theme.AppCompat.Light.Dialog</item>
        <item name="android:windowTitleStyle">@style/MyDialogTitleStyle</item>
    </style>
</resources>
